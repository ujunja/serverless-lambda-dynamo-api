service: lswn-aws-node-express-dynamode-api
frameworkVersion: '3'

custom:
  tableName: 'users-table-${sls:stage}'

provider:
  name: aws
  runtime: nodejs16.x
  iam:
    role:
      statements:
        - Effect: Allow
          Action:
            - dynamodb:Query
            - dynamodb:Scan
            - dynamodb:GetItem
            - dynamodb:PutItem
            - dynamodb:UpdateItem
            - dynamodb:DeleteItem
          Resource:
            - Fn::GetAtt: [ UsersTable, Arn ]
            # UserTableからArn呼び出し
  environment:
    LSWN_TABLE: LSWN-TABLE # 4番ライン参照

functions:
  create: # API名
    handler: handler/createHandler.createHandler
    events:
      - http: # httpApi (x), http (o)
          path: dynamo
          method: post
          cors: true
  list: # API名
    handler: handler/listHandler.listHandler
    events:
      - http: # httpApi (x), http (o)
          path: dynamo
          method: get
          cors: true

resources:
  Resources:
    UsersTable:
      Type: AWS::DynamoDB::Table
      Properties:
        AttributeDefinitions:
          - AttributeName: userId
            AttributeType: S
        KeySchema:
          - AttributeName: userId
            KeyType: HASH
        BillingMode: PAY_PER_REQUEST
        TableName: LSWN-TABLE
